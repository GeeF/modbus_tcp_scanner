[project]
name = "modbus-tcp-scan"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pymodbus>=3.13",
    "textual>=8.2",
]
